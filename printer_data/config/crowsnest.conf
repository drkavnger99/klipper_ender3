#### crowsnest.conf
#### This is a typical default config.
#### Also used as default in mainsail / MainsailOS
#### See:
#### https://github.com/mainsail-crew/crowsnest/blob/master/README.md
#### for details to configure to your needs.


#####################################################################
####                                                            #####
####      Information about ports and according URL's           #####
####                                                            #####
#####################################################################
####                                                            #####
####    Port 8080 equals /webcam/?action=[stream/snapshot]      #####
####    Port 8081 equals /webcam2/?action=[stream/snapshot]     #####
####    Port 8082 equals /webcam3/?action=[stream/snapshot]     #####
####    Port 8083 equals /webcam4/?action=[stream/snapshot]     #####
####                                                            #####
####    Note: These ports are default for most Mainsail         #####
####    installations. To use any other port would involve      #####
####    changing the proxy configuration or using directly      #####
####    http://<ip>:<port>/?action=[stream/snapshot]            #####
####                                                            #####
#####################################################################
####    RTSP Stream URL: ( if enabled and supported )           #####
####    rtsp://<ip>:<rtsp_port>/stream.h264                     #####
#####################################################################

[crowsnest]
log_path: /home/drkavnger99/printer_data/logs/crowsnest.log
log_level: verbose
delete_log: false
no_proxy: false

[cam 1]
mode: camera-streamer                 # Use camera-streamer (WebRTC)
device: /dev/v4l/by-id/usb-046d_HD_Pro_Webcam_C920_A5C5FAFF-video-index0
port: 8080                            # HTTP UI / WebRTC signaling
resolution: 1920x1080                 # C920 sweet spot (also works at 800x600)
max_fps: 30
# For C920 pick one of these formats (start with MJPEG; it avoids CPU re-encode):
custom_flags: --camera-format=YUYV
#custom_flags: --camera-format=MJPEG
# If you prefer hardware H.264 from the C920, use the next line instead of the one above:
# custom_flags: --camera-format=H264
# Optional RTSP (not needed for Mainsail WebRTC):
# enable_rtsp: true
# rtsp_port: 8554

#[crowsnest]
#log_path: /home/drkavnger99/printer_data/logs/crowsnest.log
#log_level: verbose                      # Valid Options are quiet/verbose/debug
#delete_log: false                       # Deletes log on every restart, if set to true
#no_proxy: false

#[cam 1]
#mode: camera-streamer                         # ustreamer - Provides mjpg and snapshots. (All devices)
#mode: ustreamer                                       # camera-streamer - Provides webrtc, mjpg and snapshots. (rpi + Raspi OS based only)
#enable_rtsp: true                      # If camera-streamer is used, this enables also usage of an rtsp server
#rtsp_port: 8554                         # Set different ports for each device!
#port: 8080                              # HTTP/MJPG Stream/Snapshot Port
#device: /dev/video0                     # See Log for available ...
#device: /dev/v4l/by-id/usb-046d_HD_Pro_Webcam_C920_A5C5FAFF-video-index0
#resolution: 1280x720                 # widthxheight format
#resolution: 800x600
#max_fps: 10                             # If Hardware Supports this it will be forced, otherwise ignored/coerced.
#custom_flags: --format=uyvy                         # You can run the Stream Services with custom flags.
#v4l2ctl:                               # Add v4l2-ctl parameters to setup your camera, see Log what your cam is capable of.
#custom_flags: --camera-format=H264
#[cam 2]
#mode: ustreamer                         # ustreamer - Provides mjpg and snapshots. (All devices)
#format: mjpeg
                                        # camera-streamer - Provides webrtc, mjpg and snapshots. (rpi + Raspi OS based only)
#enable_rtsp: false                      # If camera-streamer is used, this enables also usage of an rtsp server
#rtsp_port: 8554                         # Set different ports for each device!
#port: 8080                              # HTTP/MJPG Stream/Snapshot Port
#device: /dev/video0                     # See Log for available ...
#resolution: 1280x720                 # widthxheight format
#max_fps: 10                            # If Hardware Supports this it will be forced, otherwise ignored/coerced.
#custom_flags: --format=uyvy                         # You can run the Stream Services with custom flags.
#v4l2ctl:  